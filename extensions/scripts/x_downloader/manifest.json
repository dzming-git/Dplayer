{
  "id": "x_downloader",
  "name": "X（推特）媒体下载器",
  "description": "解析 X/Twitter 推文，提取其中的图片与视频；若包含多个媒体，二次触发用户选择要下载哪些。需安装 yt-dlp（pip install yt-dlp[default]）。",
  "runtime": "python",
  "command": "run.py",
  "interface": "x_download",
  "timeout": 600,
  "enabled": true,
  "params": [
    {
      "name": "url",
      "type": "string",
      "label": "推文链接",
      "required": true,
      "default": "https://x.com/7CafeLLatte/status/2048717559407743367?s=20",
      "description": "要下载的 X/Twitter 推文 URL"
    },
    {
      "name": "cookie",
      "type": "string",
      "label": "Cookie（可选）",
      "required": false,
      "description": "登录后的 Cookie 请求头原文（即浏览器里 Cookie 字段的值，不含 'Cookie: ' 前缀）。留空使用访客身份，可能受限。"
    },
    {
      "name": "library_id",
      "type": "library_select",
      "label": "目标资源库",
      "media_type": "video",
      "required": true,
      "description": "下载的视频将移动并入库到该资源库；图片也会暂存到该资源库目录"
    },
    {
      "name": "simulate",
      "type": "bool",
      "label": "演示模式（不联网）",
      "default": false,
      "description": "仅用于演示“二次选择”交互流程，不会真实下载，会生成占位文件"
    }
  ]
}
