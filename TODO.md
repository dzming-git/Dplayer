# TODO

## 待实现功能

### 标签关联标签集（资源库）

**需求描述：**
- 视频属于哪个资源库，就只能使用该资源库的标签集
- 每个标签需要关联一个资源库（标签集）
- 显示标签时，根据当前激活的资源库筛选对应标签
- 避免不同资源库的同名标签冲突

**设计思路：**
- 方案1（推荐）：Tag 表增加 `library_id` 字段
  - 内部用资源库实现，对外使用"标签集"术语
  - 创建标签时让用户选择归属的"标签集"
  - 视频标签页显示"标签集"筛选（不提资源库）
- 需要修改数据库结构

**状态：** ✅ 已完成

**落地说明：** Tag 模型已预留 `library_id` 字段并实现隔离。`/api/tags/all` 按 `library_id` 筛选（返回该库标签 + 全局标签）；创建标签唯一性按 `(path, library_id)` 判断，支持不同库同名；创建/编辑标签对话框新增「标签集（资源库）」下拉；视频页面标签编辑器已按当前激活资源库筛选对应标签集。详见 `docs/development/TODO.md`。

---

## 已完成功能

### 多级标签支持 ✅
- 支持无限层级的树形标签结构
- 标签管理页面支持展开/收起
- 视频数量递归统计（含子标签）

### 标签权限控制 ✅
- 修复了未登录用户看不到标签的问题
- 新增 `/api/tags/all` 接口获取所有标签
